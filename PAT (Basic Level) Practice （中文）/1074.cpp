#include <cstdio>
#include <cstring>

int main(int argc, char *argv[])
{
    freopen("in", "r", stdin);
    char j[22];
    char a[22];
    char b[22];
    char res[22];
    scanf("%s\n%s\n%s", j, a, b);
    printf("%s\n%s\n%s", j, a, b);
    int lj = strlen(j), la = strlen(a), lb = strlen(b);
    if()
    int tmp, jin = 0;
    for(int i = lj-1; i >= 0; --i){     

    }
    return 0;
}
