#include <cstdio>

int main(int argc, char *argv[])
{
    freopen("in", "r", stdin);
    int N;
    scanf("%d", &N);
    int a[100], b[100];

    for(int i = 0; i < N; ++i)
        scanf("%d", &a[i]);
    for(int i = 0; i < N; ++i)
        scanf("%d", &b[i]);
    
    
    return 0;
}
